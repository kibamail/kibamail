apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: infisical
spec:
  provider:
    infisical:
      secretsScope:
        projectSlug: production-kibamail-com-vu-rq
        environmentSlug: prod
