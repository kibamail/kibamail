apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cloudflare-secret.yaml
  - cluster-issuer.yaml
  - selfsigned-issuer.yaml
  - certificate.yaml
  - tracking-certificate.yaml
