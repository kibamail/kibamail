apiVersion: v1
kind: Service
metadata:
  name: kibamail-marketing
  labels:
    app.kubernetes.io/name: kibamail-marketing
    app.kubernetes.io/component: marketing
    app.kubernetes.io/part-of: kibamail
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kibamail-marketing
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
