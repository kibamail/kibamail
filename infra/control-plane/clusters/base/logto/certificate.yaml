apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: logto-tls
  namespace: logto
spec:
  secretName: logto-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - auth.kibamail.com
    - admin.auth.kibamail.com
