apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - secrets
  - storage
  - ingress
  - postgres
  - rabbitmq
  - valkey
  - outpost
  - logto
  - certificates
  # New consolidated kibamail namespace (replaces kibamail-control-plane, kibamail-marketing, kibamail-api)
  - kibamail
  - kibamail-workers
  - kibamail-tracking
  - deployment-rbac
  - signoz-infra
  - agent-monitor-rbac
