apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: kibamail-postgres-daily
spec:
  schedule: "0 0 2 * * *"
  backupOwnerReference: self
  cluster:
    name: kibamail-postgres
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
