apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: kibamail-postgres
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: kibamail-postgres
  podMetricsEndpoints:
    - port: metrics
