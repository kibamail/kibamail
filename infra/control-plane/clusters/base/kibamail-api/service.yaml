apiVersion: v1
kind: Service
metadata:
  name: kibamail-api
  namespace: kibamail-api
  labels:
    app.kubernetes.io/name: kibamail-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: kibamail
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: kibamail-api
