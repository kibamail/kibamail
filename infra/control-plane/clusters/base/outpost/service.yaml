apiVersion: v1
kind: Service
metadata:
  name: outpost
  labels:
    app.kubernetes.io/name: outpost
    app.kubernetes.io/part-of: kibamail
spec:
  type: ClusterIP
  ports:
    - port: 3333
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: outpost-api
