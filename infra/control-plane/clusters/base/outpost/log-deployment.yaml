apiVersion: apps/v1
kind: Deployment
metadata:
  name: outpost-log
  labels:
    app.kubernetes.io/name: outpost-log
    app.kubernetes.io/component: log
    app.kubernetes.io/part-of: kibamail
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: outpost-log
  template:
    metadata:
      labels:
        app.kubernetes.io/name: outpost-log
        app.kubernetes.io/component: log
        app.kubernetes.io/part-of: kibamail
    spec:
      containers:
        - name: outpost-log
          image: hookdeck/outpost:v0.7.0
          imagePullPolicy: IfNotPresent
          env:
            - name: SERVICE
              value: log
            - name: API_KEY
              valueFrom:
                secretKeyRef:
                  name: outpost-credentials
                  key: API_KEY
            - name: API_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: outpost-credentials
                  key: API_JWT_SECRET
            - name: AES_ENCRYPTION_SECRET
              valueFrom:
                secretKeyRef:
                  name: outpost-credentials
                  key: AES_ENCRYPTION_SECRET
            - name: POSTGRES_URL
              valueFrom:
                secretKeyRef:
                  name: outpost-postgres
                  key: POSTGRES_URL
            - name: RABBITMQ_SERVER_URL
              valueFrom:
                secretKeyRef:
                  name: outpost-rabbitmq
                  key: RABBITMQ_SERVER_URL
            - name: REDIS_HOST
              value: "kibamail-valkey.kibamail-valkey.svc.cluster.local"
            - name: REDIS_PORT
              value: "6379"
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: outpost-valkey-password
                  key: password
            - name: REDIS_CLUSTER_ENABLED
              value: "true"
          volumeMounts:
            - name: config
              mountPath: /config/outpost
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: config
          configMap:
            name: outpost-config
