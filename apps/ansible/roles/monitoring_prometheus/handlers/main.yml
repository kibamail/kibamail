# monitoring_prometheus Role Handlers
---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Restart prometheus
  ansible.builtin.systemd:
    name: prometheus
    state: restarted

- name: Reload prometheus
  ansible.builtin.systemd:
    name: prometheus
    state: reloaded
