# Vagrant
.vagrant/

# Ansible
.ansible_cache/
*.retry

# TLS certificates and secrets
.tls/
*.key
*.pem
*.nkeys

# Cluster credentials (per-inventory)
**/credentials/

# SSH keys and secrets (per-inventory)
**/secrets/*
!**/secrets/.gitkeep

# Local overrides
*.local.yml
