import { EmptyPage } from "@/app/(docs)/docs/_components/empty-page";

export default function NamecheapDnsPage() {
  return (
    <EmptyPage
      title="Namecheap DNS Setup"
      description="Step-by-step guide to configure DNS records for Kibamail using Namecheap."
      category="DNS Setup"
    />
  );
}
