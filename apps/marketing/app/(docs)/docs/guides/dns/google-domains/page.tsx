import { EmptyPage } from "@/app/(docs)/docs/_components/empty-page";

export default function GoogleDomainsDnsPage() {
  return (
    <EmptyPage
      title="Google Domains DNS Setup"
      description="Step-by-step guide to configure DNS records for Kibamail using Google Domains."
      category="DNS Setup"
    />
  );
}
