import { EmptyPage } from "../_components/empty-page";

export default function ContactPropertiesPage() {
  return (
    <EmptyPage
      title="Custom Properties"
      description="Extend contact profiles with custom properties for strings, numbers, and dates to store any data you need."
      category="Core Concepts"
    />
  );
}
