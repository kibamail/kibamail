import { EmptyPage } from "../_components/empty-page";

export default function SegmentsPage() {
  return (
    <EmptyPage
      title="Segments"
      description="Create dynamic and static segments to group contacts based on shared criteria for targeted campaigns."
      category="Core Concepts"
    />
  );
}
