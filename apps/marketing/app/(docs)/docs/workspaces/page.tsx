import { EmptyPage } from "../_components/empty-page";

export default function WorkspacesPage() {
  return (
    <EmptyPage
      title="Workspaces"
      description="Manage workspaces, team members, invitations, and organization settings."
      category="Settings"
    />
  );
}
