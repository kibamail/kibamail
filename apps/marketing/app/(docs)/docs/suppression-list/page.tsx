import { EmptyPage } from "../_components/empty-page";

export default function SuppressionListPage() {
  return (
    <EmptyPage
      title="Suppression List"
      description="Manage contacts who should not receive emails due to bounces, complaints, or manual suppression."
      category="Analytics & Events"
    />
  );
}
