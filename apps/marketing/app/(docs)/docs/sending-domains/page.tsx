import { EmptyPage } from "../_components/empty-page";

export default function SendingDomainsPage() {
  return (
    <EmptyPage
      title="Sending Domains"
      description="Configure and verify your sending domains with DKIM, SPF, DMARC, and tracking domain setup."
      category="Sending Infrastructure"
    />
  );
}
