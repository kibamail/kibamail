import { EmptyPage } from "../_components/empty-page";

export default function FormBuilderPage() {
  return (
    <EmptyPage
      title="Form Builder"
      description="Build forms with our drag-and-drop editor, configure field types, validation, and customize the appearance."
      category="Forms"
    />
  );
}
