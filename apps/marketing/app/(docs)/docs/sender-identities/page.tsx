import { EmptyPage } from "../_components/empty-page";

export default function SenderIdentitiesPage() {
  return (
    <EmptyPage
      title="Sender Identities"
      description="Create and manage sender identities with verified email addresses for sending broadcasts."
      category="Sending Infrastructure"
    />
  );
}
